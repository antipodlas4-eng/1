<!DOCTYPE html>
<html lang="pl">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Szczegóły - <%= property.name %></title>
  <style>
    body { font-family: 'Segoe UI', sans-serif; background: #f4f7f6; color: #333; padding: 20px; }
    .container { max-width: 800px; margin: 0 auto; }
    .card { background: white; padding: 25px; border-radius: 12px; box-shadow: 0 4px 6px rgba(0,0,0,0.1); margin-bottom: 20px; }
    h1 { color: #2c3e50; }
    .btn { display: inline-block; padding: 10px 20px; border-radius: 6px; text-decoration: none; cursor: pointer; border: none; font-size: 14px; }
    .btn-back { background: #95a5a6; color: white; margin-bottom: 20px; }
    .btn-add { background: #2ecc71; color: white; width: 100%; }
    table { width: 100%; border-collapse: collapse; margin-top: 20px; }
    th, td { padding: 12px; text-align: left; border-bottom: 1px solid #eee; }
    .status-paid { color: #27ae60; font-weight: bold; }
    .status-unpaid { color: #c0392b; font-weight: bold; }
    .arrears-total { font-size: 1.2rem; color: #c0392b; margin-top: 20px; font-weight: bold; }
  </style>
</head>
<body>
  <div class="container">
    <a href="/" class="btn btn-back">← Powrót do listy</a>
    
    <div class="card">
      <h1><%= property.name %></h1>
      <p><strong>Płatnik:</strong> <%= property.payerDetails %></p>
      <p><strong>E-mail:</strong> <a href="mailto:<%= property.payerEmail %>"><%= property.payerEmail %></a></p>
      <p><strong>Stały czynsz:</strong> <%= property.rentAmount.toFixed(2) %> PLN</p>
      <p><strong>Media/Uwagi:</strong> <%= property.utilities %></p>
    </div>

    <div class="card">
      <h3>Podwyższ czynsz</h3>
      <form method="POST" action="/property/<%= id %>/update-rent" style="display: flex; gap: 10px; align-items: flex-end;">
        <div style="flex: 1;">
          <label style="display: block; margin-bottom: 5px; font-size: 0.9rem;">Wartość</label>
          <input type="number" name="value" step="0.01" required style="width: 100%; padding: 8px; border-radius: 4px; border: 1px solid #ddd;">
        </div>
        <div style="flex: 1;">
          <label style="display: block; margin-bottom: 5px; font-size: 0.9rem;">Typ zmiany</label>
          <select name="updateType" style="width: 100%; padding: 8px; border-radius: 4px; border: 1px solid #ddd;">
            <option value="percentage">Procent (%)</option>
            <option value="amount">Kwota (PLN)</option>
          </select>
        </div>
        <button type="submit" class="btn" style="background: #e67e22; color: white; padding: 10px;">Aktualizuj</button>
      </form>
    </div>

    <div class="card">
      <h3>Dodaj kolejny miesiąc</h3>
      <form method="POST" action="/property/<%= id %>/add-month">
        <label style="display: block; margin-bottom: 10px;">
          <input type="checkbox" name="rentPaid"> Czy czynsz został opłacony?
        </label>
        <button type="submit" class="btn btn-add">Zatwierdź nowy miesiąc</button>
      </form>
    </div>

    <div class="card">
      <h3>Historia płatności</h3>
      <table>
        <thead>
          <tr>
            <th>Miesiąc</th>
            <th>Kwota</th>
            <th>Status</th>
          </tr>
        </thead>
        <tbody>
          <% 
            let totalArrears = 0;
            property.history.slice().reverse().forEach(entry => { 
              if(!entry.paid) totalArrears += entry.amount;
          %>
            <tr>
              <td><%= entry.month %></td>
              <td><%= entry.amount.toFixed(2) %> PLN</td>
              <td class="<%= entry.paid ? 'status-paid' : 'status-unpaid' %>">
                <%= entry.paid ? 'Zapłacono' : 'Zaległość' %>
              </td>
            </tr>
          <% }) %>
        </tbody>
      </table>
      
      <div class="arrears-total">
        Suma zaległości: <%= totalArrears.toFixed(2) %> PLN
      </div>
    </div>
  </div>
</body>
</html>